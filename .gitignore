# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# turborepo
.turbo

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
# .env.production holds only NEXT_PUBLIC_* (public, browser-exposed) values, so it
# is safe to commit and lets git-based deploys pick up the backend URL automatically.
!.env.production

# vercel
.vercel

# firebase
.firebase/

# typescript
*.tsbuildinfo
next-env.d.ts
